body {
    background: #001614;
    height: 100vh;
    margin: 0;
    overflow: hidden;
    font-family: 'Anton', sans-serif;
}

@keyframes spin {
    0% { transform: rotate(0deg); }
    50% { transform: scale(1, 1.5); }
    100% { transform: rotate(360deg); }
}

#circle {
    background: linear-gradient(to right, aquamarine, #a0ff27);
    height: 34vmax;
    width: 34vmax;
    position: absolute;
    top: 20%;
    left: 20%;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    animation: spin 20s infinite;
    opacity: 0.8;
}

#overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    backdrop-filter: blur(12vmax);
    z-index: 2;
}

h1 {
    font-size: clamp(2rem, 5vw, 15rem);
    color: #fff;
    white-space: nowrap;
    padding: 0 clamp(1rem, 2vw, 3rem);
    border-radius: clamp(0.4rem, 0.75vw, 1rem);
    margin: 0;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    z-index: 3;
    cursor: pointer;
    font-weight: 500;
}
